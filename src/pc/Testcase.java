package pc;

import java.io.InputStream;
import java.util.HashMap;
import java.util.Map;

import net.interfaces.HttpCallBack;
import net.utils.CommonUtils;
import net.utils.HttpUtils;

public class Testcase {

    public static void main(String[] args) {
        String testMobile = "15692423283";
        // XmlNode rootNode=new XmlNode("root");
        // rootNode.addAttribute("attr", "root");
        // XmlNode childNode=new XmlNode("path");
        // childNode.setContent("this is a content");
        // childNode.addAttribute("attr", "/user/taro/path");
        // rootNode.addSubNode(childNode);
        //
        // XmlNode grandChildNode=new XmlNode("grandChild");
        // grandChildNode.setContent("hello");
        // childNode.addSubNode(grandChildNode);
        //
        // XmlParser.createNewXml(rootNode, "/Users/taro/Desktop/test.xml",
        // "UTF-8");
    }

}
